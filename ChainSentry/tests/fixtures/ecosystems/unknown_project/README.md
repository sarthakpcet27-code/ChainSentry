# unknown_project

A fixture directory with no recognized package manager manifests or lockfiles.

Used by `test_ecosystems.py` to verify that ChainSentry correctly returns an empty ecosystem detection result when no supported manifest is present.